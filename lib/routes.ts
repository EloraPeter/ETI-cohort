/** Central app route constants — import these instead of hardcoding paths. */
export const ROUTES = {
  home: "/",
  register: "/register",
  login: "/login",
  dashboard: "/dashboard",
  accountSetup: "/account/setup",
} as const;
